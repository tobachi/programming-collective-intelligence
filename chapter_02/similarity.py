#%%
from math import sqrt

sqrt(pow(5-4, 2) + pow(4-1, 2))

#%%
1 / (1 + sqrt(pow(5-4, 2) + pow(4-1, 2)))

#%%
from chapter_02 import recommendations
recommendations.sim_distance(recommendations.critics, 'Lisa Rose', 'Gene Seymour')

#%%
